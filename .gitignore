# Lean / Lake build artifacts
/lean/.lake/
/lean/build/
**/.lake/
*.olean
*.ilean
*.trace

# LaTeX build artifacts (the PDF and generated tables are reproducible via `make`)
paper/*.aux
paper/friendly/*.aux
paper/friendly/*.log
paper/friendly/*.out
paper/friendly/*.toc
paper/friendly/*.fls
paper/friendly/*.fdb_latexmk
paper/friendly/*.synctex.gz
# paper/friendly/erdos1112-friendly.pdf is committed, like the parent paper's PDF.
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.toc
paper/build.log
paper/table-a.tex
paper/table-b.tex
# paper/erdos1112.pdf is committed: it is the submission artifact.
# It is fully reproducible with `make` (which regenerates and re-verifies the tables).

# Editor / OS
.DS_Store
*.swp

# generated by paper/scripts/make_tables_file.py
paper/scripts/probes2/

# python bytecode
__pycache__/
